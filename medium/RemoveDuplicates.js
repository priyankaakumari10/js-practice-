const numbers = [1, 2, 2, 3, 4, 4, 5];
const ar=[];

// const ans = numbers.map((num)=> ar.find(num));

// numbers.forEach((num)=>{
//     if(ans.find(num)){
//         ans.push(num);
//     }
// })


console.log(ar);